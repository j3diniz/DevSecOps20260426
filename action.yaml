name: 'Soma'
author: 'J3di'
description: 'Somar dois números.'
inputs:
  a:
    required: true
  b:
    required: true
runs:
  using: 'composite'
  steps:
    - name: setup Python
      uses: actions/setup-python@v4
      with:
        python-version: '3.10'

    - name: run Soma
      shell: bash
      run: python soma.py -d=${{ inputs.a }} -l=${{ inputs.b }}
